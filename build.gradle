apply plugin: 'java'
apply plugin: 'application'

mainClassName = 'main.java.server.HttpServer'

jar {
    baseName = 'http-server'
    version =  '0.1.0'
    manifest {
        attributes 'Main-Class': 'main.java.server.HttpServer'
    }
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.3'
}
