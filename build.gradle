apply plugin: 'java'
apply plugin: 'application'

mainClassName = 'echo.EchoServer'

jar {
    baseName = 'http-server'
    version =  '0.1.0'
    manifest {
        attributes 'Main-Class': 'server.HttpServer'
    }
}

task wrapper(type: Wrapper) {
    gradleVersion = '1.11'
}
